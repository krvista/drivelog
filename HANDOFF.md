# 인수인계 메모 (최종 갱신 2026-09-07 밤, 집 PC / WSL 세션)

다른 PC 나 새 세션에서 이 작업을 이어받을 때 먼저 읽는 문서다.
도구의 사용법과 설계 근거는 `TOOLING.md` 에 있다. 여기에는 **왜 지금 이 상태인지**와
**아직 안 끝난 것**만 적는다.

## 현재 상태를 확인하는 법

이 문서의 숫자는 작성 시점의 것이라 낡을 수 있다. 상태의 진실은 항상 원격 브랜치다.
아래를 먼저 돌려서 실제 상태를 확인할 것.

```bash
bash drivelog.sh sync   --profile ccnc   # 먼저 이 PC 의 도구를 브랜치 사본으로 맞춘다
bash drivelog.sh status --profile ccnc   # 원격/기기 파일 수
bash drivelog.sh list   --profile ccnc   # 아직 안 올라간 것 (route 별)
```

다른 PC 에서 작업하다 왔으면 `sync` 부터 돌릴 것. 도구 사본이 낡으면 조용히
기능이 빠진 채로 동작한다 (2026-09-08 실제로 겪었다).

`wk2-drivelog` 에는 아직 데이터가 없다 (도구 파일만 있다).

## 세션 기록

### 회사 PC 세션 (2026-09-07 낮)

기존 업로드 스크립트가 cowork 환경과 함께 사라져서 처음부터 다시 만들었다.
`drivelog.sh` 하나로 통합했고 `status` / `list` / `upload` / `put` / `prune` 서브커맨드를 가진다.

가장 중요한 설계 결정은 **로컬에 리포지토리 사본을 두지 않는 것**이다.
업로드 PC 가 집과 회사를 오가기 때문에, 로컬 사본이 있으면 PC 간 상태가 어긋난다.
그래서 작업 클론을 매번 만들고 지우며, "무엇이 이미 올라갔는가" 는 원격 트리 목록과
`pruned.txt` 만 보고 판단한다. 브랜치에 5.7 GB 가 있어도 작업 클론은 100 KB 대다.

구현상의 함정(`ls-tree -l`, `write-tree`, thin pack)은 `TOOLING.md` 에 정리해 뒀다.

### 집 PC 세션 (2026-09-07 밤)

**prune 정렬 문제를 해결했다** (아래 "해결된 과제" 참고). `init-order` 서브커맨드가 추가됐다.

집 PC 쪽 환경 차이를 `drivelog.conf` 에 반영했다. 회사 PC 설정은 건드리지 않았다.

### 회사 PC 세션 (2026-09-08 오전)

집에서 갱신한 문서와 코드를 읽고 나머지를 거기에 맞췄다.

- `TOOLING.md` 가 아직 "prune 정렬 기준 (미해결)" 이라고 적고 있어 이 문서와
  정면으로 어긋났다. `order.txt` 설계로 다시 썼고 `pick` / `init-order` / `sync` 를 채웠다.
- `README.md` 가 사라진 Python 스크립트 세 개를 계속 안내하고 있었다. 새로 썼다.
- `drivelog.conf.example` 에 SSH 원격 선택지와 `BATCH_FILES` 조정 지침을 넣었다.
- `status` 가 원장 상태를 보여준다. 비어 있으면 `init-order` 를 안내한다.
- `prune` 은 push 거부 시 재시도하지 않고 중단하도록 명시했다. 유지 목록을
  `$BASE` 시점으로 미리 정하므로, 그 사이 올라온 파일을 모른 채 다시 쌓으면
  그 파일을 지워버린다. 다시 실행해 처음부터 판단하는 편이 안전하다.

**낡은 도구 사본 사고를 하나 발견해 고쳤다.** 자세한 내용은 `TOOLING.md` 의
"도구 사본만은 낡을 수 있다" 절에 있다. 요약하면, 회사 PC 의 `drivelog.sh` 가
`order.txt` 를 모르던 판이어서 route `00000007`(40개 파일)을 올리면서 원장을
갱신하지 않았다. 그대로 두면 `prune` 이 가장 최신 주행을 먼저 지웠을 것이다.
`init-order --yes` 로 히스토리에서 복원했고(19개 route, 기존 18개는 그대로),
재발을 막으려고 도구 버전 비교 경고와 `sync` 서브커맨드를 넣었다.

## 환경 (확인된 사실)

### 공통

- comma 기기의 dongle 은 `5494f8f29b7fd585` 이며 `ccnc` 프로필 = `ccnc-drivelog` 브랜치다.
- 기기 SSH 는 GitHub 계정에 등록된 공개키로 인증한다. 기기는 **SSH Keys 에 username 을
  입력한 그 시점에** `github.com/<user>.keys` 를 한 번 받아온다. 나중에 키를 추가하면
  기기는 그것을 모르므로, 기기 설정에서 username 을 다시 입력해 갱신해야 한다.
  갱신하면 두 PC 의 키가 함께 들어오므로 한쪽이 밀려나지 않는다.

### GitHub 에 등록된 SSH 키 두 개 — 둘 다 사용 중이다

**이전 판에 "예전 키는 지워도 된다"고 적혀 있었으나 그것은 잘못이다. 지우면 안 된다.**

- `SHA256:pAPISSQen6KnkFo/XdWkGrR19UT93nr7zbDE65XtSeQ` — **집 PC 의 키다.**
  회사 세션에서 짝이 되는 개인키를 찾지 못한 것은 당연하다. 개인키는 집 PC 의
  `~/.ssh/` 에 있다. 이 키로 집 PC 는 GitHub push 와 기기 SSH 접속을 모두 하고 있다.
- 나머지 하나가 회사 PC 의 키다.

### 회사 PC (`GRAM-Ryzen26`)

- 경로 `/mnt/e/upload_drivelog`, Python 없음, WSL Ubuntu.
- **HTTPS + Windows 자격 증명 관리자**를 쓴다. 설정 방법은 `TOOLING.md` 의 WSL 절 참고.
- 기기 IP `192.168.1.31`.

### 집 PC (`GRAMPRO-2025`)

- 경로 `/mnt/d/K.Studio/download_drivelog`.
- **SSH 원격을 쓴다** (`git@github.com:krvista/drivelog.git`). 공개키가 등록돼 있어
  HTTPS 자격증명 설정이 필요 없다.
- `~/.ssh/config` 에 **github.com 을 `ssh.github.com:443` 으로 보내는 설정**이 있다.
  포트 22 로 대용량 push 가 20~40초 지점에서 반복해서 끊겨 우회한 것이다.
  되돌리려면 그 블록을 지우면 된다 (표식으로 감싸 두었다).
- `BATCH_FILES=4` 로 낮췄다. 기본값 12 는 한 번에 약 130 MB 인데 이 회선에서는
  자주 끊긴다. 4 개(약 45 MB)면 10 초 안에 끝나 성공률이 높다.
  회선이 안정되면 8~12 로 올려도 된다.
- 기기 IP 는 집 네트워크 기준이라 회사와 다르다. `drivelog.conf` 를 고치거나
  `--host` 를 쓸 것.
- **`~/K.Studio/drivelog` 에 예전 방식의 전체 클론(5.7 GB)이 아직 남아 있다.**
  새 도구는 이것을 쓰지 않는다. 다만 브랜치 히스토리 재작성(용량 회수)을 하게 되면
  기존 blob 이 이미 로컬에 있는 이 클론이 유용하므로, 그 방침을 정하기 전까지 두었다.

## 해결된 과제: prune 의 정렬 기준

**해결됨 (2026-09-07 밤, 집 PC).**

문제는 이랬다. `prune` 이 route 이름 문자열 순으로 오래된 것을 판단했는데,
기기를 다시 빌드하면 route 카운터가 `00000000` 부터 다시 시작한다.
2026-09-06 밤 빌드 후 새 데이터가 `00000005` 로 시작했고 브랜치에는 `00000061` 까지
있었으므로, 그대로 돌리면 가장 최신 주행이 먼저 지워질 상황이었다.

날짜로 바꾸는 것도 답이 아니다. 주차장에서만 움직이면 GPS 를 못 잡아 기기 시각이
어긋난다. **재빌드 직후 지하주차장 주행이면 이름과 시각이 동시에 깨진다.**
어느 한쪽을 믿고 다른 쪽을 보정하는 방식으로는 이 경우를 풀 수 없다.

그래서 판단 근거를 기기 밖으로 옮겼다. **업로드 순서**다.
기기 이름도 기기 시각도 안 보고, 우리가 언제 올렸는지만 본다. 출퇴근길에 모아서
올리므로 업로드 순서가 곧 시간 순서이고, 재빌드에도 시계 오차에도 흔들리지 않는다.

- 브랜치 루트의 `order.txt` 에 route 를 업로드된 순서대로 기록한다.
- `upload` 이 데이터 파일과 **같은 커밋**에 원장을 갱신한다. 중간에 끊겨도
  "올라갔는데 원장에 없는" 상태가 생기지 않는다.
- 두 PC 가 동시에 올려도 먼저 push 한 쪽의 순서가 보존되고 뒤에 덧붙는다.
- `prune` 은 이 순서로만 판단한다.
- 원장에 없는 route(구 도구로 올린 것)는 가장 오래된 것으로 취급한다.

기존 데이터의 순서는 추정하지 않는다. git 히스토리에 각 파일이 어느 커밋에서
처음 추가됐는지 남아 있고 그것이 곧 업로드 시점이다. `init-order` 가 그것을 복원한다.

**아직 `init-order` 를 돌리지 않았다면 먼저 실행할 것.**

```bash
bash drivelog.sh init-order --profile ccnc --dry-run   # 복원될 순서 확인
bash drivelog.sh init-order --profile ccnc             # 실제 생성
```

`order.txt` 가 없으면 `prune` 은 이름순으로 되돌아가며 실행 시 경고를 띄운다.

## 남은 과제

### 1. 저장 용량

`ccnc-drivelog` 는 약 5.7 GB 로 GitHub 권장 한도(5 GB)를 넘겼다.
`prune` 은 브랜치 tip 에서 파일을 덜어낼 뿐 히스토리는 남으므로 GitHub 이 실제로
쓰는 용량은 줄지 않는다. 용량을 되찾으려면 히스토리를 다시 써야 하고, 그러려면
기존 blob 을 전부 로컬에 가지고 있어야 한다.

집 PC 의 `~/K.Studio/drivelog` 전체 클론이 그 조건을 이미 만족한다. 이 작업을
할 생각이면 그 클론을 지우지 말 것.

### 2. GitHub 업로드가 자주 끊긴다 (집 PC)

집 회선에서 GitHub 으로 대용량 push 가 20~40초 지점에서 반복해서 끊긴다.
확인한 것:

- 회선 자체는 정상이다. Cloudflare 로 200 MB 업로드가 18초에 완주한다 (11 MB/s).
- 포트 22 와 443 모두 같은 증상이다.
- 새로 만든 빈 저장소로도 10 MB 부터 실패한다. 즉 저장소 크기와 무관하다.
- 1 KB 는 통과한다. 크기에 의존한다.

GitHub 쪽 제한으로 보인다. 배치를 작게(4 개, 약 45 MB) 하면 대체로 통과한다.
계속 실패하면 시간을 두고 재시도하는 편이 낫다.
진단 스크립트는 집 PC 의 `/mnt/d/K.Studio/download_drivelog` 에 있다
(`diag_network.sh`, `test_push_size.sh`, `test_sustained_upload.sh`).

### 3. wk2 브랜치

`wk2-drivelog` 는 아직 데이터가 없다. WK2 차량 기기의 SSH 주소와 dongle 을 확인한 적이
없어서 `drivelog.conf` 의 해당 항목이 비어 있다. 처음 접속에 성공하면 `status` 가
실제 dongle 을 알려주므로 `PROFILE_DONGLE[wk2]` 에 넣어둘 것.
차가 두 대인데 스크립트는 하나이므로, 이 값을 채워야 프로필을 잘못 줬을 때
엉뚱한 브랜치로 데이터가 들어가는 사고를 막을 수 있다.

## 주의

`drivelog.conf` 는 PC 마다 다르므로 브랜치에 올리지 않는다.
새 PC 에서는 `drivelog.conf.example` 을 복사해 기기 주소와 원격 URL 을 채울 것.
