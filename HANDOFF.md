# 인수인계 메모 (2026-09-07, 회사 PC / WSL 세션)

다른 PC 나 새 세션에서 이 작업을 이어받을 때 먼저 읽는 문서다.
도구의 사용법과 설계 근거는 `TOOLING.md` 에 있다. 여기에는 **왜 지금 이 상태인지**와
**아직 안 끝난 것**만 적는다.

## 현재 상태를 확인하는 법

이 문서의 숫자는 작성 시점의 것이라 낡을 수 있다. 상태의 진실은 항상 원격 브랜치다.
아래를 먼저 돌려서 실제 상태를 확인할 것.

```bash
bash drivelog.sh status --profile ccnc   # 원격/기기 파일 수
bash drivelog.sh list   --profile ccnc   # 아직 안 올라간 것 (route 별)
```

작성 시점 기준으로 `ccnc-drivelog` 에 563개 파일이 있다. 2026-09-06 밤 빌드 후
첫 출근길 주행분인 route `00000005--e1271d2b50` 의 38개(483 MB)를 이 세션에서 올려
기존 525개에 더해진 결과다. 기기에 남은 미업로드는 없었다.

`wk2-drivelog` 에는 아직 데이터가 없다 (도구 파일만 있다).

## 이번 세션에서 한 일

기존 업로드 스크립트가 cowork 환경과 함께 사라져서 처음부터 다시 만들었다.
예전 스크립트(`upload_ssh_drivelog.py` 등)는 이제 없다. `drivelog.sh` 하나로 통합했고
`status` / `list` / `upload` / `put` / `prune` 서브커맨드를 가진다.

가장 중요한 설계 결정은 **로컬에 리포지토리 사본을 두지 않는 것**이다.
업로드 PC 가 집과 회사를 오가기 때문에, 로컬 사본이 있으면 PC 간 상태가 어긋난다.
그래서 작업 클론을 매번 만들고 지우며, "무엇이 이미 올라갔는가" 는 원격 트리 목록과
`pruned.txt` 만 보고 판단한다. 로컬 상태 파일이 없으므로 어느 PC 에서 실행하든
같은 답이 나온다. 브랜치에 5.7 GB 가 있어도 작업 클론은 100 KB 대다.
구현상의 함정(`ls-tree -l`, `write-tree`, thin pack)은 `TOOLING.md` 에 정리해 뒀다.

도구 파일 자체를 두 브랜치에 올려 뒀다. 코드가 다시 사라지지 않게 하려는 것이다.
`drivelog.conf` 는 PC 마다 다르므로 올리지 않는다 (`drivelog.conf.example` 을 복사해 쓸 것).

## 환경 (확인된 사실)

- 업로드 PC 는 WSL Ubuntu (`GRAM-Ryzen26`) 에서 작업한다. 경로는 `/mnt/e/upload_drivelog`.
  Python 은 설치돼 있지 않다 (Windows 스토어 스텁뿐). 그래서 도구는 bash 로 작성했다.
- WSL 은 Windows 자격 증명 관리자를 자동으로 쓰지 않는다. `credential.helper` 를
  Windows 쪽 GCM 으로 연결해 해결했다. 설정 방법은 `TOOLING.md` 의 WSL 절 참고.
  **집 PC 에서도 WSL 을 쓴다면 같은 설정이 필요하다.**
- comma 기기 `192.168.1.31` 은 C4-CE1N 기기이고 `ccnc` 프로필에 해당한다.
  dongle 은 `5494f8f29b7fd585` 이며 `ccnc-drivelog` 의 기존 525개도 같은 기기에서 나왔다.
  **집 네트워크에서는 IP 가 다를 것이다.** `drivelog.conf` 를 고치거나 `--host` 를 쓸 것.
- 기기 SSH 는 GitHub 계정에 등록된 공개키로 인증한다. 이 PC 의 키를 계정에 추가해서
  통과시켰다. 집 PC 의 키도 같은 방식으로 등록해야 기기에 붙을 수 있다.
- GitHub 계정에는 이 PC 키 외에 예전 키가 하나 더 등록돼 있다
  (`SHA256:pAPISSQen6KnkFo/XdWkGrR19UT93nr7zbDE65XtSeQ`).
  짝이 되는 개인키를 어디서도 찾지 못했다. cowork 와 함께 사라진 것으로 보인다.
  더 이상 쓸 데가 없으면 계정에서 지워도 된다.

## 남은 과제

### 1. prune 의 정렬 기준 (집에서 고치기로 함)

가장 급한 것이다. 자세한 내용은 `TOOLING.md` 의 "알려진 문제" 절에 있다. 요약하면,

- `prune` 은 route 이름 문자열 순으로 오래된 것을 판단한다.
- 그런데 기기를 다시 빌드하면 route 카운터가 `00000000` 부터 다시 시작한다.
  실제로 2026-09-06 밤 빌드 후 새 데이터가 `00000005` 로 시작했다.
  브랜치에는 `00000061` 까지 있으므로, 지금 prune 을 돌리면 **최신 주행이 먼저 지워진다.**
- 날짜 기준으로 바꾸는 것도 답이 아니다. 주차장에서만 움직이면 GPS 를 못 잡아
  기기 시각이 실제와 어긋난다.

즉 route 이름과 기기 시각 모두 신뢰할 수 없다. 업로드 시점에 순서를 별도로 기록해 두는
방식(예: 원격에 순번 원장을 남긴다)을 포함해 다시 설계해야 한다.
**고치기 전까지 `prune --yes` 를 쓰지 말 것.** 기본값은 계획만 출력하므로 안전하다.

### 2. 저장 용량

`ccnc-drivelog` 는 파일 525개, 개당 약 11 MB 로 약 5.7 GB 다. GitHub 권장 한도(5 GB)를
이미 넘겼다. `prune` 은 브랜치 tip 에서 파일을 덜어낼 뿐 히스토리는 남으므로
GitHub 이 실제로 쓰는 용량은 줄지 않는다. 용량을 되찾으려면 히스토리를 다시 써야 하고,
그러려면 기존 blob 을 전부 로컬에 받아야 한다(수 GB). 대역폭이 넉넉한 쪽에서 할 작업이다.
1번 과제와 같이 묶어서 판단하는 편이 낫다.

### 3. wk2 브랜치

`wk2-drivelog` 는 아직 데이터가 없다. WK2 차량 기기의 SSH 주소와 dongle 을 확인한 적이
없어서 `drivelog.conf` 의 해당 항목이 비어 있다. 처음 접속에 성공하면 `status` 가
실제 dongle 을 알려주므로 `PROFILE_DONGLE[wk2]` 에 넣어둘 것.
차가 두 대인데 스크립트는 하나이므로, 이 값을 채워야 프로필을 잘못 줬을 때
엉뚱한 브랜치로 데이터가 들어가는 사고를 막을 수 있다.
